Now I have all the evidence needed. Let me compile the analysis.

```json
{
  summary code-review-agent 是一个为仓库分析和代码审查场景设计的轻量级 Agent 运行时框架，基于 FastAPI 提供 HTTP API 和 Web UI，采用 DeepSeek 优先的模型适配策略，实现了模型抽象、工具注册与执行、ReAct 风格 Agent 循环、运行生命周期管理、事件追溯以及仓库分析应用（Repo Analyst）等核心能力。,
  modules [
    {
      name messages（消息层）,
      description 定义 provider-neutral 的消息类型（Message、Role、ToolCall、ToolResult）及辅助构造器。Runtime 所有内部逻辑依赖此类型，而非 vendor 原始 payload，为多模型适配提供稳定边界。
    },
    {
      name models（模型层）,
      description 抽象 ChatModel 接口，提供 ChatRequest  ChatResponse  ModelUsage 等类型。内置 OpenAICompatibleModel 适配器（通过 httpx 调用非流式 chat completions API），以及 DeepSeekModel 子类（自动读取环境变量 DEEPSEEK_API_KEY）。
    },
    {
      name formatters（格式化层）,
      description 实现 MessageFormatter 协议和 OpenAIChatFormatter，负责将内部消息工具 schema 转换为 OpenAI-compatible payload，以及解析 provider 返回的 assistant 消息和 tool_calls。
    },
    {
      name tools（工具层）,
      description 定义 Tool 抽象基类、ToolRegistry（注册查询schema导出执行）、ToolContext（携带 workspace_root、run_id 等）。内置三个文件工具：list_files（递归globlimit隐藏过滤）、read_file（行范围截断非UTF-8替换标记）、search_text（自动 rg→Python 降级搜索）。
    },
    {
      name sandbox（沙箱层）,
      description 提供最小路径保护函数 resolve_workspace_path，拒绝绝对路径和 .. 越界，确保文件工具只能在 workspace_root 边界内操作。
    },
    {
      name session（会话层）,
      description 定义 Session 抽象接口和 InMemorySession 实现，管理 Agent 运行过程中的消息列表（追加获取清空），所有消息均 deep-copy 以确保不可变。
    },
    {
      name harness（Agent 循环层）,
      description 实现 Agent 类，运行 ReAct 风格循环：模型生成→解析 tool_calls→注册表调用工具→结果回写会话→继续迭代直到模型返回纯文本或达到 max_iterations。输出结构化 AgentRunResult（statusstepsusageiterations）。
    },
    {
      name runtime（运行时层）,
      description 实现 AgentRuntime 服务，封装 run 创建、异步执行、状态管理、事件追加。内置默认运行时工厂（DeepSeekModel + 三个文件工具）。使用 InMemoryRunStore 管理 RunRecord 和 RunEvent。
    },
    {
      name api（API 层）,
      description 基于 FastAPI 的应用工厂和路由定义。暴露健康检查、通用 Agent run CRUD（runs）、Repo Analyst 专用 API（repo-analystruns）以及静态 Web UI 页面。后台异步执行 run。
    },
    {
      name appsrepo_analyst（仓库分析应用）,
      description Repo Analyst 专用应用层。提供 RepoAnalystService（facade over 通用 runtime）、专用提示词构造（要求工具探查后输出结构化 JSON）、报告解析器（从 final_message 提取 RepoAnalystReport 并校验 schema）。
    },
    {
      name web（前端 UI）,
      description 内嵌单页 HTML 应用（中文界面），提供三栏布局：提交表单（仓库路径问题参数）、任务列表、详情面板（结构化报告 + 事件流）。通过 fetch API 与 FastAPI 后端交互。
    },
    {
      name storage（存储层，占位）,
      description 当前仅包含 __init__.py 占位，README 计划后续实现 SQLite 持久化存储，目前所有数据存储在 InMemoryRunStore。
    },
    {
      name observability（可观测性，占位）,
      description 当前仅包含 __init__.py 占位，README 计划后续实现运行时事件流和追踪工具。
    }
  ],
  architecture [
    分层解耦架构：messages → modelsformatters → toolssandbox → session → harness(Agent) → runtime(AgentRuntime) → apiapps，下层不依赖上层，各层通过 Pydantic model 和抽象接口解耦。,
    Provider-neutral 设计：内部所有逻辑依赖 Message  ChatModel  Tool 抽象，通过 OpenAIChatFormatter 适配 OpenAI-compatible API，通过 ChatModel 接口对接 DeepSeek其他模型。,
    ReAct Agent 循环：Agent.run() 在 max_iterations 内循环：模型推理→若含 tool_calls 则注册表执行→工具结果转 tool_message 回写会话→再次请求模型→直到模型返回纯文本回复。,
    运行生命周期管理：AgentRuntime.create_run() → status='queued' → execute_run() → status='running' → 完成失败max_iterations。每次状态变更和每一步 agent step 都记录为 RunEvent，支持全链路追溯。,
    应用层隔离模式：Repo Analyst 作为独立 app 运行在通用 runtime 之上，通过 app_name 过滤 run，通过专用提示词和结果解析器将通用 AgentRunResult 转换为领域特定 RepoAnalystReport。,
    异步执行：API 层通过 BackgroundTasks 后台执行 run，立即返回 run_id 给调用方，客户端轮询 runs{id}events 获取进展。,
    Web UI 三栏实时交互：左侧表单提交任务，中间列表显示历史任务，右侧面板展示结构化报告和事件详情，通过轮询自动刷新。,
    工具沙箱安全边界：所有文件工具通过 resolve_workspace_path 强制限制在 workspace_root 内，拒绝绝对路径和 .. 逃逸；搜索工具自动降级（rg → Python）避免单点故障。
  ],
  risks [
    InMemoryRunStore 无持久化：所有 run 记录和事件存储在内存字典中，服务重启后数据丢失。README 中计划的 SQLite 存储（storage 模块）目前仅为占位。,
    Observability 模块未实现：README 计划的可观测事件流追踪功能当前仅为空包，生产环境中缺少指标监控和链路追踪能力。,
    Web UI 缺少 SSEWebSocket 实时推送：当前采用前端轮询机制，高并发场景下轮询效率低，缺少事件驱动推送。,
    无认证授权机制：API 端点完全开放，未实现任何鉴权，直接暴露文件系统操作能力（workspace_root 参数控制），存在安全风险。,
    模型调用缺少超时重试优雅降级：OpenAICompatibleModel 虽有基础超时，但缺少重试策略、速率限制处理和 fallback 模型切换。,
    Shell 工具未实现：README 计划的安全 shell 执行策略尚未开发，限制了代码审查自动化场景（如运行测试、lint）。,
    deepseek-chat 模型未经验证：DeepSeekModel 当前指向 deepseek-chat，但无真实 API 集成测试，模型名兼容性存疑。,
    Storage 模块和 SQLAlchemy 依赖已声明但未使用：pyproject.toml 声明了 sqlalchemy 和 aiosqlite，但 storage 模块为空，存在无用依赖和未实现的技术债。,
    错误处理边界：Agent 循环中 tool 执行异常被 registry 统一捕获为 ToolExecutionResult.error，但模型异常直接触发 Agent 返回 failed，缺少局部重试能力。,
    单线程限制：InMemoryRunStore 使用 RLock 虽保证线程安全，但异步环境下并发执行多个 run 时会因 GIL 和锁竞争影响性能。
  ],
  next_steps [
    实现 SQLite 持久化存储（storage 模块）：基于 SQLAlchemy 完成 RunRecord 和 RunEvent 的数据库模型和 CRUD，替代 InMemoryRunStore，支持服务重启后数据恢复。,
    添加模型调用的超时重试与 fallback：为 OpenAICompatibleModel 增加指数退避重试、速率限制处理，并支持配置 fallback 模型列表实现自动降级。,
    实现 Shell 安全执行工具（sandboxcommand）：设计安全策略（允许命令白名单黑名单、超时、环境隔离），实现 run_commandshell 工具，扩展代码审查自动化能力。,
    增加 API 认证和请求验证：为 FastAPI 端点添加 API Key 或 JWT 认证，对 workspace_root 参数添加更严格的路径校验（仅允许已注册的仓库路径）。,
    实现 SSEWebSocket 实时事件推送：替代当前轮询机制，让客户端订阅 run 事件流，降低延迟和网络开销。,
    实现可观测性模块（observability）：添加结构化日志、OpenTelemetry 集成、运行时指标暴露（metrics），支持生产环境监控。,
    增加多模型厂商支持：基于 OpenAICompatibleModel 添加 SiliconFlow、OpenRouter、Ollama（本地）等 provider 适配，允许运行时动态切换。,
    完善测试覆盖：增加 API 集成测试（test_api.py 当前测试框架到位但需补充用例）、Agent 循环边界测试（max_iterations、多工具调用链）、端到端 smoke test。
  ]
}